{
  "name": "crane-landing",
  "private": true,
  "description": "Static landing page for crane, served from crane.byabhay.com via Cloudflare Workers.",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
